from aiogram import Router
from aiogram.filters import Command
from aiogram.types import Message

router = Router()


@router.message(Command("start", "help", prefix="!/"))
async def cmd_start(message: Message):
    text = (
        "╔══════════════════════════════╗\n"
        "║  🏦 <b>БАНКОВСКАЯ СИСТЕМА RP</b>  ║\n"
        "╚══════════════════════════════╝\n\n"

        "━━━ 🔹 <b>ПРОФЕССИИ</b> ━━━━\n"
        "💼 <code>!работа</code> — список профессий (по категориям)\n"
        "📝 <code>!устроиться Название</code> — подать заявку на работу\n"
        "👤 <code>!моя_работа</code> — моя профессия\n"
        "🚫 <code>!уволиться</code> — уволиться\n"
        "💰 <code>!зп @user1 @user2 ...</code> — выплатить зарплату (админ)\n"
        "💵 <code>!изменить_зп Название сумма</code> (админ)\n"
        "🚛 <code>!доставка</code> — совершить доставку (дальнобойщик)\n\n"

        "━━━━ 🔹 <b>ДЕНЬГИ</b> ━━━━\n"
        "💳 <code>!баланс</code> — баланс\n"
        "💸 <code>!перевести @user сумма</code> — перевод\n"
        "📜 <code>!история</code> — история операций\n"
        "📊 <code>!рейтинг</code> — топ\n\n"

        "━━━ 🔹 <b>КРЕДИТЫ / ВКЛАДЫ</b> ━━━\n"
        "🏦 <code>!запросить_кредит сумма</code>\n"
        "💳 <code>!кредиты</code> / 💵 <code>!погасить id сумма</code>\n"
        "📈 <code>!запросить_вклад сумма</code>\n"
        "🏧 <code>!вклады</code> / 💰 <code>!вывести id</code>\n\n"

        "━━━ 🔹 <b>АВТОМОБИЛИ</b> ━━━\n"
        "🚗 <code>!авто</code> — список (салон + Б/У)\n"
        "ℹ️ <code>!авто_инфо номер</code> — инфо\n"
        "🛒 <code>!купить номер</code> / 💲 <code>!продать_авто НОМЕР</code> (слить в гос 60%)\n"
        "🚙 <code>!мои_авто</code> — мои авто\n"
        "🏷 <code>!продать НОМЕР цена</code> — выставить игрокам\n"
        "🚫 <code>!снять_продажу НОМЕР</code> — убрать из продажи\n"
        "🎁 <code>!контейнер</code> — открыть контейнер с авто ($25,000)\n\n"

        "━━━ 🔹 <b>ДОМА</b> ━━━\n"
        "🏠 <code>!дома [район]</code> — список (с фильтром по району)\n"
        "ℹ️ <code>!дом НОМЕР</code> / 🏡 <code>!купить_дом НОМЕР</code>\n"
        "🏘️ <code>!мои_дома</code> — мои дома\n"
        "💲 <code>!продать_дом НОМЕР</code> — слить в гос (65%)\n"
        "🏷 <code>!продать_дом НОМЕР цена</code> — выставить игрокам\n"
        "🚫 <code>!снять_продажу_дома НОМЕР</code> — убрать из продажи\n"
        "🎁 <code>!контейнер_дом [район]</code> — контейнер с домом ($50,000)\n"
        "🗺 <code>!районы</code> — список районов\n\n"

        "━━━ 🔹 <b>АРЕНДА НЕДВИЖИМОСТИ</b> ━━━\n"
        "🏠 <code>!аренда_дома [район]</code> — дома в аренду\n"
        "🔑 <code>!арендовать НОМЕР</code> — арендовать дом\n"
        "📋 <code>!моя_аренда</code> — моя аренда\n"
        "🚪 <code>!съехать</code> — съехать\n"
        "💰 <code>!сдать_дом НОМЕР цена</code> — сдать свой дом (владелец)\n"
        "🚫 <code>!снять_с_аренды НОМЕР</code> — снять с аренды (владелец)\n\n"

        "━━━ 🔹 <b>АРЕНДА АВТО</b> ━━━\n"
        "🚗 <code>!аренда_авто</code> — авто в аренду\n"
        "🔑 <code>!арендовать_авто НОМЕР</code> — арендовать авто\n"
        "📋 <code>!моя_аренда_авто</code> — моя аренда\n"
        "🚗 <code>!вернуть_авто</code> — вернуть авто\n"
        "💰 <code>!сдать_авто НОМЕР цена</code> — сдать своё авто (владелец)\n"
        "🚫 <code>!снять_с_аренды_авто НОМЕР</code> — снять с аренды (владелец)\n\n"

        "━━━ 🔹 <b>ОРГАНИЗАЦИИ</b> ━━━\n"
        "🏢 <code>!создать_орг Название</code> — создать\n"
        "📋 <code>!орги</code> — мои организации\n"
        "ℹ️ <code>!орг ID</code> — инфо и баланс\n"
        "💰 <code>!орг_пополнить ID сумма</code> — пополнить с баланса\n"
        "💳 <code>!орг_вывести ID сумма</code> — вывести (владелец)\n"
        "👥 <code>!орг_добавить ID @user</code> — добавить участника\n"
        "🚫 <code>!орг_удалить ID @user</code> — удалить\n"
        "🚗 <code>!орг_авто ID</code> — имущество (авто)\n"
        "🏠 <code>!орг_дома ID</code> — имущество (дома)\n"
        "🚛 <code>!орг_прицепы ID</code> — имущество (прицепы)\n"
        "💸 Оплата из орг: <code>!купить 5 орг 1</code>\n"
        "💸 В собственность орг: <code>!купить 5 орг 1 вл</code>\n\n"

        "━━━ 🔹 <b>ТОПЛИВО</b> ━━━\n"
        "⛽ <code>!цена_топлива [сумма]</code> — установить цену (админ)\n"
        "🛢 <code>!заправить количество</code> — оплатить топливо\n\n"

        "━━━ 🔹 <b>ПРИЦЕПЫ (Truck Planet)</b> ━━━\n"
        "🚛 <code>!прицепы</code> — список прицепов\n"
        "ℹ️ <code>!прицеп_инфо НОМЕР</code> — информация\n"
        "🛒 <code>!купить_прицеп НОМЕР</code> — купить\n"
        "💲 <code>!продать_прицеп ID</code> — слить (60%)\n"
        "🚙 <code>!мои_прицепы</code> — мои прицепы\n"
        "🏷 <code>!продать_прицеп_игроку ID цена</code> — выставить игрокам\n"
        "🚫 <code>!снять_продажу_прицепа ID</code> — убрать из продажи\n"
        "🎁 <code>!контейнер_прицеп</code> — контейнер ($5,000)\n\n"

        "━━━━━ 🔹 <b>АДМИНАМ</b> ━━━━━\n"
        "💰 <code>!начислить</code> / <code>!списать</code> / <code>!установить_баланс</code>\n"
        "💵 <code>!стартовый_баланс [сумма]</code>\n"
        "🔄 <code>!сброс [сумма]</code> — сброс всех балансов\n"
        "🎁 <code>!выдать_стартовый</code> — стартовый баланс юзерам с 0\n"
        "👤 <code>!обновить_имена</code> / 🗑 <code>!очистить_старые</code>\n"
        "📊 <code>!статистика</code> — статистика чата\n"
        "📢 <code>!объявления</code> — настройка постинга\n"
        "🚗 <code>!объявления машины вкл/выкл/тест/интервал N</code>\n"
        "🏠 <code>!объявления дома вкл/выкл/тест/интервал N</code>\n"
        "🚛 <code>!объявления прицепы вкл/выкл/тест/интервал N</code>\n"
        "📺 <code>!объявления канал @channel</code> — канал для машин\n"
        "🏘 <code>!объявления дома_канал @channel</code> — канал для домов\n"
        "🗑️ <code>!очистить_объявления</code>\n"
        "👤 <code>!авто_пользователя @user</code>\n"
        "🚫 <code>!изъять_авто id</code> / 🎁 <code>!выдать_авто @user id</code>\n"
        "🗑️ <code>!очистить_авто @user</code> — очистить все авто пользователя\n"
        "📋 <code>!все_авто</code> — все купленные авто\n"
        "🏆 <code>!премия @user кол-во такси|автобус</code> — премия за заказы\n"
        "👑 <code>!заявки_на_работу</code> — список заявок на профессии\n"
        "✅ <code>!принять_на_работу ID</code> — одобрить заявку\n"
        "❌ <code>!отказать_на_работу ID</code> — отклонить заявку\n"
        "🚷 <code>!уволить @user</code> — снять с должности\n"
        "📊 <code>!работы</code> — список всех сотрудников (по профессиям)\n\n"
        "🏠 <code>!добавить_дом</code> / <code>!удалить_дом id</code>\n"
        "✅ <code>!одобрить_кредит id % дней</code>\n"
        "✅ <code>!одобрить_вклад id % дней</code>\n"
        "❌ <code>!отклонить_кредит id</code> / <code>!отклонить_вклад id</code>\n"
        "📋 <code>!заявки</code> / <code>!все_кредиты</code> / <code>!все_вклады</code>\n\n"
        "━━━━━━━━━━━━━━━━━━━━━━━\n"
        "🔐 <i>Админ-команды доступны только администраторам группы</i>"
    )
    await message.reply(text, parse_mode="HTML")
